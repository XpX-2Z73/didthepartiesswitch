{# Primary Sources Cards #}
<div class="primary-carousel">
  {% for source in primarySources %}
    <article class="primary-card">
      <header>
        <span>{{ source.date }}</span>
        <h3>{{ source.title }}</h3>
      </header>
      <blockquote>
        "{{ source.quote }}"
      </blockquote>
      <footer>{{ source.attribution }}</footer>
      <a href="{{ source.link }}" target="_blank" rel="noopener noreferrer">
        {{ source.linkText }}
      </a>
      {% if source.link2 %}
      <a href="{{ source.link2 }}" target="_blank" rel="noopener noreferrer">
        {{ source.linkText2 }}
      </a>
      {% endif %}
      {% if source.archiveLink %}
      <a href="{{ source.archiveLink }}" target="_blank" rel="noopener noreferrer" class="primary-card__archive">
        Archived copy
      </a>
      {% endif %}
      {% if source.removedByGovernment %}
      <p class="primary-card__removed">&#9888; Government source removed in 2025. {{ source.removalNote }}</p>
      {% endif %}
    </article>
  {% endfor %}
</div>
